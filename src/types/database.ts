// Placeholder for generated Supabase types. Replace with `supabase gen types typescript` output.
export type Database = Record<string, unknown>;
export type Json = string | number | boolean | null | { [key: string]: Json } | Json[];
